
spring:

  main:
    allow-bean-definition-overriding: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_sns?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: kk23782378

  servlet:
    session:
      timeout: 54000
      cookie:
        http-only: true


  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 600000
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 2


  session:
    store-type: redis
    redis:
      flush-mode: on_save
      namespace: spring:session

